# Changelog

All notable changes to this project are documented here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Set the release version with `uv version X.Y.Z`, record the release here, and
tag the merged commit `vX.Y.Z`. Pushing the matching tag builds and publishes
that version.

## [Unreleased]

### Added

- Bhu-Naksha client, location listing, plot crawl and owner parse as the
  `rajasthan_ror` package with `rajasthan-ror-list`, `rajasthan-ror-fetch` and
  `rajasthan-ror-parse` console scripts.
- Rajasthan schedule synonyms shipped as schema-versioned JSON and a jati
  string categoriser over them.

[Unreleased]: https://github.com/in-rolls/rajasthan-ror/commits/main
